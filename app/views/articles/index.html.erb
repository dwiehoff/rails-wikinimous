<h1>Articles</h1>

<ul>
  <% @articles.each do |a| %>
    <li> <%= link_to a.title, article_path(a) %> [ <%= link_to "Edit", edit_article_path(a) %> | <%= link_to "Delete", article_path(a), method: :delete, data: { confirm: "Are you absolutely sure?" }  %> ]</li>
  <% end %>
</ul>

<%= link_to "Add article", new_article_path %>
